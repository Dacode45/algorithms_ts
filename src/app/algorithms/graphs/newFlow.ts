import { Graph } from "../../datastructures/graph";

export function netFlow(g: Graph, source: number, sink: number) {
    // bfs to look for any path from source to sink that increase flow,
    // augment the total. terminate with optimal flow when none is found

    
}